{
  "pythondevshellflake": {
    "prefix": "pythondevshellflake",
    "body": [
      "{",
      "  inputs = {",
      "    flake-parts = {",
      "      url = \"github:hercules-ci/flake-parts\";",
      "      inputs.nixpkgs-lib.follows = \"nixpkgs\";",
      "    };",
      "    nixpkgs.url = \"github:NixOS/nixpkgs/nixos-24.05\";",
      "  };",
      "",
      "  outputs = inputs@{ self, flake-parts, ... }:",
      "    flake-parts.lib.mkFlake { inherit inputs; } {",
      "      systems = [ \"aarch64-darwin\" \"aarch64-linux\" \"x86_64-linux\" ];",
      "",
      "      perSystem = { pkgs, ... }:",
      "        let",
      "          py3 = pkgs.python3.override",
      "            {",
      "              packageOverrides = self: super: { };",
      "            };",
      "          venv = py3.withPackages (ps: with ps; [",
      "            ipython",
      "          ]);",
      "        in",
      "        {",
      "          devShells.default = pkgs.mkShell {",
      "            packages = [",
      "              venv",
      "            ];",
      "            shellHook = ''",
      "              GIT_WC=\\$(git rev-parse --show-toplevel)",
      "",
      "              export PYTHONPATH=''\\${GIT_WC}/src",
      "            '';",
      "          };",
      "        };",
      "    };",
      "}"
    ],
    "description": "Nix Flake configuration with Python development shell"
  },
  "vimplugin": {
    "prefix": "vimplugin",
    "body": [
      "{ vimUtils, fetchFromGitHub }:",
      "",
      "vimUtils.buildVimPlugin {",
      "  pname = \"${1:plugin-name}\";",
      "  version = \"${2:0-unstable-yyyy-mm-dd}\";",
      "  src = fetchFromGitHub {",
      "    owner = \"${3:owner}\";",
      "    repo = \"${1:plugin-name}\";",
      "    rev = \"${5:rev}\";",
      "    hash = \"\";",
      "  };",
      "}"
    ],
    "description": "Nix derivation for a Vim plugin"
  }
}
